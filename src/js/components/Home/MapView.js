import React, { Component, PropTypes } from 'react';

class MapView extends Component {
  render() {
    return (
      <div>
      </div>
    );
  }
}

MapView.propTypes = {
};

export default MapView;
